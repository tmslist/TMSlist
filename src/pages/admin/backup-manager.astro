---
export const prerender = false;
import Layout from "../../layouts/Layout.astro";
import AdminSidebar from "../../components/react/AdminSidebar";
import AdminBackupManager from "../../components/react/AdminBackupManager";
import { getSessionFromRequest, hasRole } from "../../utils/auth";
const session = getSessionFromRequest(Astro.request);
if (!hasRole(session, 'admin')) return Astro.redirect("/admin/login");
---
<Layout title="Backup Manager | TMS List Admin" noindex={true}>
  <div class="flex min-h-screen bg-gray-50">
    <AdminSidebar client:load currentPage="backup" userEmail={session!.email} />
    <main class="flex-1 lg:ml-64 py-8"><div class="container mx-auto px-4 max-w-7xl"><AdminBackupManager client:load /></div></main>
  </div>
</Layout>
