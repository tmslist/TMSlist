---
export const prerender = false;
import Layout from '../../layouts/Layout.astro';
import PortalGallery from '../../components/react/PortalGallery';
import PortalSidebar from '../../components/react/PortalSidebar';
import { getSessionFromRequest, hasRole } from '../../utils/auth';

const session = getSessionFromRequest(Astro.request);
if (!session || !hasRole(session, 'clinic_owner', 'admin', 'editor')) {
  return Astro.redirect('/portal/login/');
}
---

<Layout title="Gallery | Doctor Portal | TMS List" description="Manage your clinic photo gallery" noindex={true}>
  <div class="flex min-h-screen bg-[var(--paper2)]">
    <PortalSidebar client:load currentPage="gallery" userEmail={session.email} userId={session.userId} />
    <main class="flex-1 md:ml-64 py-8">
      <div class="container mx-auto px-4 max-w-4xl">
        <div class="mb-8">
          <h1 class="text-2xl font-bold text-[var(--ink)]">Photo Gallery</h1>
          <p class="text-[var(--muted)] mt-1">Showcase your clinic with photos of facilities and team</p>
        </div>
        <PortalGallery client:load />
      </div>
    </main>
  </div>
</Layout>