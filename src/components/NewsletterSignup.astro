---
interface Props {
  variant?: 'inline' | 'banner';
  source?: string;
}

const { variant = 'inline', source = 'footer' } = Astro.props;
---

{variant === 'banner' ? (
  <section class="bg-gradient-to-r from-blue-600 to-violet-700 py-12">
    <div class="container mx-auto px-6 lg:px-12">
      <div class="flex flex-col md:flex-row items-center justify-between gap-8">
        <div class="text-white">
          <h2 class="text-2xl font-bold mb-2">Stay Updated on TMS Therapy</h2>
          <p class="text-blue-100 font-light">Get weekly updates on new clinics, insurance coverage, and treatment advances.</p>
        </div>
        <form class="newsletter-form flex gap-3 w-full md:w-auto" data-source={source}>
          <input type="email" name="email" required placeholder="your@email.com"
            class="flex-1 md:w-72 px-5 py-3.5 rounded-xl text-sm bg-white/10 backdrop-blur border border-white/20 text-white placeholder-blue-200 focus:bg-white/20 focus:border-white/40 focus:ring-0 transition-all" />
          <button type="submit" class="px-6 py-3.5 bg-white text-blue-700 font-bold rounded-xl text-sm hover:bg-blue-50 transition-all shrink-0">
            Subscribe
          </button>
        </form>
      </div>
      <p class="newsletter-success hidden text-emerald-200 font-medium mt-4 text-center md:text-left">Subscribed! Check your inbox for a welcome email.</p>
      <p class="newsletter-error hidden text-red-200 font-medium mt-4 text-center md:text-left"></p>
    </div>
  </section>
) : (
  <div class="bg-slate-50 rounded-2xl border border-slate-100 p-6">
    <h3 class="text-lg font-bold text-slate-900 mb-2">TMS Newsletter</h3>
    <p class="text-sm text-slate-500 mb-4">Weekly updates on clinics, insurance, and research.</p>
    <form class="newsletter-form flex gap-2" data-source={source}>
      <input type="email" name="email" required placeholder="your@email.com"
        class="flex-1 px-4 py-2.5 rounded-xl border border-slate-200 text-sm focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all" />
      <button type="submit" class="px-5 py-2.5 bg-blue-600 hover:bg-blue-700 text-white font-bold rounded-xl text-sm transition-all shrink-0">
        Join
      </button>
    </form>
    <p class="newsletter-success hidden text-emerald-600 text-sm font-medium mt-3">Subscribed!</p>
    <p class="newsletter-error hidden text-red-600 text-sm font-medium mt-3"></p>
  </div>
)}

<script>
  document.querySelectorAll('.newsletter-form').forEach(form => {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const f = e.target as HTMLFormElement;
      const parent = f.closest('section, div')!;
      const email = (f.querySelector('[name="email"]') as HTMLInputElement).value;
      const source = f.dataset.source || 'website';

      try {
        const res = await fetch('/api/newsletter/subscribe', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ email, source }),
        });

        if (res.ok) {
          f.classList.add('hidden');
          parent.querySelector('.newsletter-success')?.classList.remove('hidden');
        } else {
          const { error } = await res.json();
          const errEl = parent.querySelector('.newsletter-error') as HTMLElement;
          errEl.textContent = error || 'Something went wrong';
          errEl.classList.remove('hidden');
        }
      } catch {
        const errEl = parent.querySelector('.newsletter-error') as HTMLElement;
        errEl.textContent = 'Network error. Try again.';
        errEl.classList.remove('hidden');
      }
    });
  });
</script>
