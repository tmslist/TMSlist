---
phase: 11-admin-data-layer-buildout
plan: 01
type: execute
wave: 1
depends_on: ["10-01", "10-02"]
files_modified:
  - src/db/schema.ts
  - src/db/queries.ts
  - src/pages/api/admin/clinic-claims.ts
  - src/pages/api/admin/support-tickets.ts
  - src/pages/api/admin/bulk-email-campaigns.ts
  - src/pages/api/admin/api-keys.ts
  - src/pages/api/admin/forum-moderation.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
must_haves:
  truths:
    - "Clinic claims API handles approve/reject/takeover workflow"
    - "Support tickets API supports create, update, resolve workflow"
    - "Bulk email campaigns API supports schedule, track, report"
    - "API keys API supports create, rotate, revoke for external integrations"
    - "Forum moderation API handles flag, hide, delete posts and comments"
  artifacts:
    - path: "src/pages/api/admin/clinic-claims.ts"
      provides: "Full clinic claims workflow API"
    - path: "src/pages/api/admin/support-tickets.ts"
      provides: "Support tickets management API"
    - path: "src/pages/api/admin/bulk-email-campaigns.ts"
      provides: "Bulk email campaign management API"
    - path: "src/pages/api/admin/api-keys.ts"
      provides: "API keys CRUD API"
    - path: "src/pages/api/admin/forum-moderation.ts"
      provides: "Forum moderation actions API"
---

<objective>
Build high-priority data layer components: clinicClaims, supportTickets, bulkEmailCampaigns, apiKeys, forumModeration tables and APIs.
</objective>

<context>
@src/db/schema.ts
@src/db/queries.ts
@src/db/forumQueries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Clinic Claims data layer (DATA-01)</name>
  <files>
    src/db/schema.ts
    src/db/queries.ts
    src/pages/api/admin/clinic-claims.ts
    src/components/react/AdminClaimsTracker.tsx
  </files>
  <action>
First, add `clinicClaims` table to schema if not present:

```typescript
export const clinicClaims = pgTable('clinic_claims', {
  id: uuid('id').defaultRandom().primaryKey(),
  clinicId: uuid('clinic_id').references(() => clinics.id).notNull(),
  userId: uuid('user_id').references(() => users.id).notNull(),
  status: text('status').default('pending'), // pending, verified, rejected
  npi: text('npi'),
  proofDocuments: jsonb('proof_documents').$type<string[]>(),
  verifiedAt: timestamp('verified_at'),
  verifiedBy: uuid('verified_by').references(() => users.id),
  notes: text('notes'),
  takeoverRequested: boolean('takeover_requested').default(false),
  createdAt: timestamp('created_at').defaultNow(),
  updatedAt: timestamp('updated_at').defaultNow(),
}, (table) => [
  index('idx_clinic_claims_clinic').on(table.clinicId),
  index('idx_clinic_claims_status').on(table.status),
]);
```

Create `src/pages/api/admin/clinic-claims.ts`:
```typescript
// GET - List all claims with clinic and user info
// POST - Create new claim (when user requests ownership)
// PUT /:id - Update claim status (approve/reject/verify)
// POST /:id/takeover - Execute ownership transfer
```

Create `AdminClaimsTracker.tsx` component for the admin claims page.
</action>
  <verify>
grep "clinicClaims" src/db/schema.ts
</verify>
<done>Clinic claims API handles full ownership verification workflow</done>
</task>

<task type="auto">
  <name>Task 2: Build Support Tickets data layer (DATA-02)</name>
  <files>
    src/db/schema.ts
    src/db/queries.ts
    src/pages/api/admin/support-tickets.ts
    src/components/react/AdminTicketQueue.tsx
  </files>
  <action>
Add `supportTickets` table to schema:

```typescript
export const supportTickets = pgTable('support_tickets', {
  id: uuid('id').defaultRandom().primaryKey(),
  subject: text('subject').notNull(),
  description: text('description'),
  type: text('type').default('general'), // general, technical, billing, feedback
  priority: text('priority').default('normal'), // low, normal, high, urgent
  status: text('status').default('open'), // open, in_progress, resolved, closed
  reporterEmail: text('reporter_email'),
  reporterName: text('reporter_name'),
  assignedTo: uuid('assigned_to').references(() => users.id),
  clinicId: uuid('clinic_id').references(() => clinics.id),
  messages: jsonb('messages').$type<{ from: string; text: string; at: string }[]>(),
  resolution: text('resolution'),
  closedAt: timestamp('closed_at'),
  createdAt: timestamp('created_at').defaultNow(),
  updatedAt: timestamp('updated_at').defaultNow(),
}, (table) => [
  index('idx_support_tickets_status').on(table.status),
  index('idx_support_tickets_priority').on(table.priority),
]);
```

Create API and component for ticket management.
</action>
  <verify>
grep "supportTickets" src/db/schema.ts
</verify>
<done>Support tickets API supports create, assign, respond, resolve workflow</done>
</task>

<task type="auto">
  <name>Task 3: Build Bulk Email Campaigns data layer (DATA-03)</name>
  <files>
    src/db/schema.ts
    src/pages/api/admin/bulk-email-campaigns.ts
    src/components/react/AdminEmailCampaigns.tsx
  </files>
  <action>
Add `bulkEmailCampaigns` table to schema:

```typescript
export const bulkEmailCampaigns = pgTable('bulk_email_campaigns', {
  id: uuid('id').defaultRandom().primaryKey(),
  name: text('name').notNull(),
  subject: text('subject').notNull(),
  templateId: uuid('template_id').references(() => emailTemplates.id),
  bodyHtml: text('body_html'),
  status: text('status').default('draft'), // draft, scheduled, sending, sent, failed
  scheduledAt: timestamp('scheduled_at'),
  sentAt: timestamp('sent_at'),
  totalRecipients: integer('total_recipients').default(0),
  deliveredCount: integer('delivered_count').default(0),
  openedCount: integer('opened_count').default(0),
  clickedCount: integer('clicked_count').default(0),
  bouncedCount: integer('bounced_count').default(0),
  filterCriteria: jsonb('filter_criteria').$type<Record<string, any>>(),
  createdBy: uuid('created_by').references(() => users.id),
  createdAt: timestamp('created_at').defaultNow(),
});
```

Create API for campaign CRUD and sending.
</action>
  <verify>
grep "bulkEmailCampaigns" src/db/schema.ts
</verify>
<done>Bulk email campaigns API supports create, schedule, send, and track</done>
</task>

<task type="auto">
  <name>Task 4: Build API Keys data layer (DATA-04)</name>
  <files>
    src/db/schema.ts
    src/pages/api/admin/api-keys.ts
    src/pages/admin/api-keys.astro
  </files>
  <action>
Add `apiKeys` table to schema:

```typescript
export const apiKeys = pgTable('api_keys', {
  id: uuid('id').defaultRandom().primaryKey(),
  name: text('name').notNull(),
  keyHash: text('key_hash').notNull(),
  keyPrefix: text('key_prefix').notNull(), // e.g., "tms_live_xxx" for display
  userId: uuid('user_id').references(() => users.id),
  clinicId: uuid('clinic_id').references(() => clinics.id),
  permissions: text('permissions').array().default(sql`'{}'`),
  rateLimit: integer('rate_limit').default(100),
  lastUsedAt: timestamp('last_used_at'),
  expiresAt: timestamp('expires_at'),
  revokedAt: timestamp('revoked_at'),
  createdAt: timestamp('created_at').defaultNow(),
}, (table) => [
  index('idx_api_keys_user').on(table.userId),
]);
```

Create API for CRUD operations and key generation:
- POST: Generate new API key (returns full key once, then only hash stored)
- GET: List keys (shows prefix + masked key)
- DELETE: Revoke key
- PUT: Update permissions/rate limit
</action>
  <verify>
grep "apiKeys" src/db/schema.ts
</verify>
<done>API keys management supports create, rotate, revoke workflow</done>
</task>

<task type="auto">
  <name>Task 5: Build Forum Moderation data layer (DATA-05)</name>
  <files>
    src/db/schema.ts
    src/db/forumQueries.ts
    src/pages/api/admin/forum-moderation.ts
    src/pages/admin/forum-moderation.astro
  </files>
  <action>
Check existing forum moderation in `forumQueries.ts`. Add moderation actions:

Create `src/pages/api/admin/forum-moderation.ts`:
```typescript
// GET - List flagged/reported posts and comments
// POST /flag - Flag a post or comment
// PUT /:id/hide - Hide content (moderator action)
// PUT /:id/remove - Remove content (deletion)
// PUT /:id/restore - Restore removed content
// POST /:id/warn - Send warning to user
```

Add tables if needed:
```typescript
export const forumModerationLog = pgTable('forum_moderation_log', {
  id: uuid('id').defaultRandom().primaryKey(),
  postId: uuid('post_id').references(() => forumPosts.id),
  commentId: uuid('comment_id').references(() => forumComments.id),
  action: text('action').notNull(), // flagged, hidden, removed, restored, warned
  moderatorId: uuid('moderator_id').references(() => users.id),
  reason: text('reason'),
  createdAt: timestamp('created_at').defaultNow(),
});
```

Create component for admin forum moderation queue.
</action>
  <verify>
grep "forumModeration\|moderation" src/db/schema.ts src/db/forumQueries.ts | head -10
</verify>
<done>Forum moderation API handles flag, hide, remove, restore, warn actions</done>
</task>

</tasks>

<verification>
- Clinic claims API handles full workflow
- Support tickets API works
- Bulk email campaigns API works
- API keys API works
- Forum moderation API works
</verification>

<success_criteria>
1. All 5 high-priority tables exist in schema
2. All 5 APIs handle CRUD and business logic
3. Admin components wire to APIs
</success_criteria>

<output>
After completion, create `.planning/phases/11-admin-data-layer-buildout/11-01-SUMMARY.md`
</output>