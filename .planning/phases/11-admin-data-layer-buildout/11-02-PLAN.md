---
phase: 11-admin-data-layer-buildout
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/db/schema.ts
  - src/pages/api/admin/auth-events.ts
  - src/pages/api/admin/login-history.ts
  - src/pages/api/admin/media-library.ts
  - src/pages/api/admin/experiments.ts
  - src/pages/api/admin/insurance-plans.ts
  - src/pages/api/admin/feature-flags.ts
autonomous: true
requirements:
  - DATA-06
  - DATA-07
  - DATA-08
  - DATA-09
  - DATA-10
  - DATA-11
  - DATA-12
must_haves:
  truths:
    - "Auth events page shows login history with IP/device tracking"
    - "Media library API supports upload, organize, delete images"
    - "Experiments API supports A/B test configuration"
    - "Insurance plans API supports CRUD for insurance providers"
    - "Feature flags API supports toggle features on/off"
  artifacts:
    - path: "src/pages/api/admin/auth-events.ts"
      provides: "Auth events listing API"
    - path: "src/pages/api/admin/media-library.ts"
      provides: "Media library management API"
    - path: "src/pages/api/admin/experiments.ts"
      provides: "A/B experiments API"
---

<objective>
Build medium-priority data layer components: authEvents, loginHistory, mediaLibrary, experiments, insurance, featureFlags.
</objective>

<context>
@src/db/schema.ts
@src/utils/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Auth Events data layer (DATA-06, DATA-07)</name>
  <files>
    src/pages/api/admin/auth-events.ts
    src/pages/admin/security-dashboard.astro
  </files>
  <action>
The auth_events table was already created in Phase 1 (AUTH-10). Create API to query it:

Create `src/pages/api/admin/auth-events.ts`:
- GET: List auth events with filters (user, action type, date range)
- Actions: login_success, login_failed, logout, password_reset_requested, 2fa_enabled, etc.

Use the `logAuthEvent` function from auth.ts to populate this table.
</action>
  <verify>
grep "auth_events.*pgTable" src/db/schema.ts
</verify>
<done>Auth events page shows login history with security alerts</done>
</task>

<task type="auto">
  <name>Task 2: Build Media Library data layer (DATA-08)</name>
  <files>
    src/db/schema.ts
    src/pages/api/admin/media-library.ts
    src/pages/api/upload.ts
    src/components/react/AdminMediaLibrary.tsx
  </files>
  <action>
Add `mediaAssets` table:

```typescript
export const mediaAssets = pgTable('media_assets', {
  id: uuid('id').defaultRandom().primaryKey(),
  filename: text('filename').notNull(),
  originalFilename: text('original_filename').notNull(),
  mimeType: text('mime_type').notNull(),
  sizeBytes: integer('size_bytes').notNull(),
  url: text('url').notNull(),
  thumbnailUrl: text('thumbnail_url'),
  width: integer('width'),
  height: integer('height'),
  alt: text('alt'),
  caption: text('caption'),
  tags: text('tags').array().default(sql`'{}'`),
  folder: text('folder'),
  uploadedBy: uuid('uploaded_by').references(() => users.id),
  clinicId: uuid('clinic_id').references(() => clinics.id),
  usageCount: integer('usage_count').default(0),
  createdAt: timestamp('created_at').defaultNow(),
}, (table) => [
  index('idx_media_assets_clinic').on(table.clinicId),
]);
```

Create upload API and media library management.
</action>
  <verify>
grep "mediaAssets\|media_assets" src/db/schema.ts
</verify>
<done>Media library supports upload, organize, and optimize images</done>
</task>

<task type="auto">
  <name>Task 3: Build Experiments data layer (DATA-09)</name>
  <files>
    src/db/schema.ts
    src/pages/api/admin/experiments.ts
    src/pages/admin/experiments.astro
  </files>
  <action>
Add `experiments` table:

```typescript
export const experiments = pgTable('experiments', {
  id: uuid('id').defaultRandom().primaryKey(),
  name: text('name').notNull(),
  key: text('key').notNull().unique(),
  description: text('description'),
  status: text('status').default('draft'), // draft, running, paused, completed
  variantA: jsonb('variant_a').$type<Record<string, any>>(),
  variantB: jsonb('variant_b').$type<Record<string, any>>(),
  trafficAllocation: integer('traffic_allocation').default(50), // % to variant B
  targetAudience: jsonb('target_audience').$type<Record<string, any>>(),
  startDate: timestamp('start_date'),
  endDate: timestamp('end_date'),
  results: jsonb('results').$type<{ conversionsA: number; conversionsB: number; significance: number }>(),
  createdBy: uuid('created_by').references(() => users.id),
  createdAt: timestamp('created_at').defaultNow(),
});
```

Create API for experiment CRUD and result tracking.
</action>
  <verify>
grep "experiments" src/db/schema.ts | head -5
</verify>
<done>Experiments API supports A/B test creation, running, and analysis</done>
</task>

<task type="auto">
  <name>Task 4: Build Insurance Plans data layer (DATA-10)</name>
  <files>
    src/db/schema.ts
    src/pages/api/admin/insurance-plans.ts
    src/pages/admin/insurance.astro
  </files>
  <action>
Check if insurancePlans table exists. If not:

```typescript
export const insurancePlans = pgTable('insurance_plans', {
  id: uuid('id').defaultRandom().primaryKey(),
  name: text('name').notNull(),
  provider: text('provider').notNull(),
  type: text('type'), // HMO, PPO, EPO, etc.
  coverage: text('coverage'), // description of TMS coverage
  tmsCoverage: boolean('tms_coverage').default(false),
  priorAuthRequired: boolean('prior_auth_required').default(true),
  maxSessions: integer('max_sessions'),
  copay: decimal('copay', { precision: 10, scale: 2 }),
  createdAt: timestamp('created_at').defaultNow(),
});
```

Create API and admin UI for managing insurance plans.
</action>
  <verify>
grep "insurancePlans" src/db/schema.ts | head -3
</verify>
<done>Insurance plans page supports CRUD for insurance providers</done>
</task>

<task type="auto">
  <name>Task 5: Build Feature Flags data layer (DATA-11)</name>
  <files>
    src/db/schema.ts
    src/pages/api/admin/feature-flags.ts
    src/pages/admin/feature-flags.astro
  </files>
  <action>
Add `featureFlags` table:

```typescript
export const featureFlags = pgTable('feature_flags', {
  id: uuid('id').defaultRandom().primaryKey(),
  key: text('key').notNull().unique(),
  name: text('name').notNull(),
  description: text('description'),
  enabled: boolean('enabled').default(false),
  rolloutPercentage: integer('rollout_percentage').default(0),
  targetUsers: jsonb('target_users').$type<string[]>(), // user IDs
  createdAt: timestamp('created_at').defaultNow(),
  updatedAt: timestamp('updated_at').defaultNow(),
});
```

Create API:
- GET: List all flags with status
- POST: Create new flag
- PUT /:id: Update flag (enable/disable, change rollout)
- DELETE /:id: Delete flag

In code, use feature flags:
```typescript
const isEnabled = await isFeatureEnabled('new-billing-ui');
if (isEnabled) {
  // show new UI
}
```
</action>
  <verify>
grep "featureFlags" src/db/schema.ts | head -3
</verify>
<done>Feature flags API supports gradual rollout and targeted enablement</done>
</task>

<task type="auto">
  <name>Task 6: Build Lead Routing data layer (DATA-12)</name>
  <files>
    src/db/schema.ts
    src/pages/api/admin/lead-routing.ts
    src/pages/admin/lead-routing.astro
  </files>
  <action>
Add `leadRoutingRules` table:

```typescript
export const leadRoutingRules = pgTable('lead_routing_rules', {
  id: uuid('id').defaultRandom().primaryKey(),
  name: text('name').notNull(),
  priority: integer('priority').default(0),
  conditions: jsonb('conditions').$type<{ field: string; operator: string; value: any }[]>(),
  targetClinicId: uuid('target_clinic_id').references(() => clinics.id),
  targetUserId: uuid('target_user_id').references(() => users.id),
  fallbackEnabled: boolean('fallback_enabled').default(true),
  notifyOnAssignment: boolean('notify_on_assignment').default(true),
  isActive: boolean('is_active').default(true),
  createdAt: timestamp('created_at').defaultNow(),
});
```

Create API for rule CRUD and test functionality.
</action>
  <verify>
grep "leadRoutingRules" src/db/schema.ts | head -3
</verify>
<done>Lead routing rules support automatic lead assignment based on conditions</done>
</task>

</tasks>

<verification>
- Auth events page shows security data
- Media library handles uploads
- Experiments API works
- Insurance plans CRUD works
- Feature flags toggle works
- Lead routing rules work
</verification>

<success_criteria>
1. All 7 medium-priority tables exist
2. All APIs handle CRUD operations
3. Admin components wire to APIs
</success_criteria>

<output>
After completion, create `.planning/phases/11-admin-data-layer-buildout/11-02-SUMMARY.md`
</output>