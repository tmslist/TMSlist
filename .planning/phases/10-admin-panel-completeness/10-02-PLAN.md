---
phase: 10-admin-panel-completeness
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/pages/admin/subscriptions.astro
  - src/pages/admin/jobs-applications.astro
  - src/pages/admin/notifications.astro
  - src/pages/admin/billing.astro
  - src/pages/admin/gdpr.astro
  - src/pages/admin/integrations.astro
  - src/pages/admin/audit-trail.astro
  - src/components/react/AdminSubscriptions.tsx
  - src/components/react/AdminJobsApplications.tsx
  - src/components/react/AdminNotifications.tsx
  - src/components/react/AdminBilling.tsx
autonomous: true
requirements:
  - BRIDGE-01
  - BRIDGE-02
  - BRIDGE-03
  - BRIDGE-04
  - BRIDGE-05
  - BRIDGE-06
  - BRIDGE-07
  - BRIDGE-08
  - BRIDGE-09
  - BRIDGE-10
  - BRIDGE-11
must_haves:
  truths:
    - "Subscriptions page shows all clinic subscriptions with status, plan, billing cycle"
    - "Jobs applications page shows all job applications with status management"
    - "Notifications page shows all notifications with read/unread toggle"
    - "Billing page shows invoices, payments, and outstanding balances"
    - "GDPR page shows data requests with approve/reject actions"
    - "Integrations page shows connected services with connect/disconnect"
    - "Audit trail page shows all admin actions with filtering"
  artifacts:
    - path: "src/pages/admin/subscriptions.astro"
      provides: "Subscriptions management page"
    - path: "src/pages/admin/jobs-applications.astro"
      provides: "Job applications management page"
    - path: "src/pages/admin/notifications.astro"
      provides: "Notifications management page"
    - path: "src/pages/api/admin/subscriptions.ts"
      provides: "Subscriptions CRUD API"
---

<objective>
Build API-to-UI bridges for 11 missing or incomplete pages: subscriptions, jobs applications, notifications, billing, GDPR requests, integrations, questions, treatments, audit log, SEO auditor (persistent), and newsletter.
</objective>

<context>
@src/db/schema.ts
@src/db/queries.ts
@src/pages/api/admin/subscriptions.ts
@src/pages/api/admin/jobs.ts
@src/pages/api/admin/notifications.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Subscriptions bridge (BRIDGE-01)</name>
  <files>
    src/pages/api/admin/subscriptions.ts
    src/components/react/AdminSubscriptions.tsx
    src/pages/admin/subscriptions.astro
  </files>
  <action>
Create `src/pages/api/admin/subscriptions.ts`:
- GET: List all subscriptions with clinic info, plan, status, billing dates
- POST: Create new subscription for a clinic
- PUT: Update subscription (change plan, pause, cancel)
- DELETE: Cancel subscription

Create `src/components/react/AdminSubscriptions.tsx`:
- Table with columns: Clinic, Plan, Status, MRR, Next Billing, Actions
- Filters: Status (active, paused, canceled), Plan type
- Actions: View details, Change plan, Pause/Resume, Cancel
- Stats cards: Total MRR, Active subscriptions, Churned this month

Wire into `src/pages/admin/subscriptions.astro`.
</action>
  <verify>
grep "AdminSubscriptions" src/pages/admin/subscriptions.astro
</verify>
<done>Subscriptions page shows all clinic subscriptions with management actions</done>
</task>

<task type="auto">
  <name>Task 2: Build Jobs Applications bridge (BRIDGE-02)</name>
  <files>
    src/pages/api/admin/jobs-applications.ts
    src/components/react/AdminJobsApplications.tsx
    src/pages/admin/jobs-applications.astro
  </files>
  <action>
Check if `src/pages/api/admin/jobs-applications.ts` exists. If not, create it:
- GET: List all job applications with applicant info, job title, clinic, status
- PUT: Update application status (reviewing, interview, offer, hired, rejected)

Create `AdminJobsApplications.tsx`:
- Table: Applicant name, Email, Phone, Job, Clinic, Applied date, Status
- Filters: Job, Status, Date range
- Actions: View details modal, Update status dropdown, Email applicant
- Bulk actions: Select multiple, change status

Wire into `src/pages/admin/jobs-applications.astro`.
</action>
  <verify>
grep "AdminJobsApplications" src/pages/admin/jobs-applications.astro
</verify>
<done>Jobs applications page shows all applications with status management</done>
</task>

<task type="auto">
  <name>Task 3: Build Notifications bridge (BRIDGE-03)</name>
  <files>
    src/pages/api/admin/notifications.ts
    src/components/react/AdminNotifications.tsx
    src/pages/admin/notifications.astro
  </files>
  <action>
Create `src/pages/api/admin/notifications.ts`:
- GET: List admin notifications with pagination
- PUT: Mark as read, Mark all as read
- DELETE: Delete notification

Create `AdminNotifications.tsx`:
- List of notifications with icon, title, message, time, read/unread state
- Filter: All, Unread, by type
- Actions: Mark read, Delete
- Bell icon with unread count badge

Wire into `src/pages/admin/notifications.astro`.
</action>
  <verify>
grep "AdminNotifications" src/pages/admin/notifications.astro
</verify>
<done>Notifications page shows all admin notifications with read/unread management</done>
</task>

<task type="auto">
  <name>Task 4: Build Billing bridge (BRIDGE-04)</name>
  <files>
    src/pages/api/admin/billing.ts
    src/components/react/AdminBilling.tsx
    src/pages/admin/billing.astro
  </files>
  <action>
Create `src/pages/api/admin/billing.ts`:
- GET: List all invoices with clinic, amount, status, due date
- POST: Create new invoice
- PUT: Update invoice (mark paid, add notes)
- GET /api/admin/billing/overview: Revenue summary stats

Create `AdminBilling.tsx`:
- Stats: Total revenue, Outstanding, Overdue, This month
- Invoices table: Invoice #, Clinic, Amount, Status (paid/pending/overdue), Due date
- Filters: Status, Date range, Clinic
- Actions: View invoice, Mark paid, Send reminder, Create refund

Wire into `src/pages/admin/billing.astro`.
</action>
  <verify>
grep "AdminBilling" src/pages/admin/billing.astro
</verify>
<done>Billing page shows invoices, payments, and revenue overview</done>
</task>

<task type="auto">
  <name>Task 5: Build GDPR Requests bridge (BRIDGE-05)</name>
  <files>
    src/pages/api/admin/gdpr-requests.ts
    src/components/react/AdminGDPRRequests.tsx
    src/pages/admin/gdpr.astro
  </files>
  <action>
Create `src/pages/api/admin/gdpr-requests.ts`:
- GET: List GDPR requests (export data, delete account, consent changes)
- POST: Create new request (for manual submissions)
- PUT: Process request (approve, reject), trigger data export/delete

Create `AdminGDPRRequests.tsx`:
- Table: Request type, User email, Submitted date, Status, Actions
- Request types: Data export, Account deletion, Consent update
- Status badges: Pending, Processing, Completed, Rejected
- Actions: View details, Approve, Reject, Download export

Wire into `src/pages/admin/gdpr.astro`.
</action>
  <verify>
grep "AdminGDPRRequests" src/pages/admin/gdpr.astro
</verify>
<done>GDPR page shows data requests with approve/reject workflow</done>
</task>

<task type="auto">
  <name>Task 6: Build Integrations bridge (BRIDGE-06)</name>
  <files>
    src/pages/api/admin/integrations.ts
    src/components/react/AdminIntegrations.tsx
    src/pages/admin/integrations.astro
  </files>
  <action>
Create `src/pages/api/admin/integrations.ts`:
- GET: List all integrations with connection status
- POST: Connect integration (OAuth flow or API key setup)
- DELETE: Disconnect integration

Create `AdminIntegrations.tsx`:
- Grid of integration tiles: Stripe, Zapier, Google Calendar, Mailchimp, etc.
- Each tile: Logo, name, description, Connect/Disconnect button, status
- Connected state shows: Last sync time, stats (e.g., "152 customers synced")
- Configuration modal for each integration

Wire into `src/pages/admin/integrations.astro`.
</action>
  <verify>
grep "AdminIntegrations" src/pages/admin/integrations.astro
</verify>
<done>Integrations page shows connected services with connect/disconnect</done>
</task>

<task type="auto">
  <name>Task 7: Build Questions bridge (BRIDGE-07)</name>
  <files>
    src/pages/api/admin/questions.ts
    src/components/react/AdminQuestions.tsx
    src/pages/admin/questions.astro
  </files>
  <action>
Check if questions API exists. If not, create `src/pages/api/admin/questions.ts`:
- GET: List all quiz/assessment questions
- POST: Create new question
- PUT: Update question
- DELETE: Delete question

Create `AdminQuestions.tsx`:
- Table: Question text, Type, Category, Responses, Actions
- Question types: Multiple choice, Scale, Free text
- Editor: Rich text editor for question and options
- Preview: Show how question appears to users

Wire into `src/pages/admin/questions.astro`.
</action>
  <verify>
grep "AdminQuestions" src/pages/admin/questions.astro
</verify>
<done>Questions page shows quiz questions with CRUD management</done>
</task>

<task type="auto">
  <name>Task 8: Build Treatments bridge (BRIDGE-08)</name>
  <files>
    src/pages/api/admin/treatments.ts
    src/components/react/AdminTreatments.tsx
    src/pages/admin/treatments.astro
  </files>
  <action>
Check if treatments API exists. If not, create `src/pages/api/admin/treatments.ts`:
- GET: List all treatments with descriptions
- POST: Create new treatment
- PUT: Update treatment
- DELETE: Delete treatment

Create `AdminTreatments.tsx`:
- Table: Treatment name, Description, Category, Clinic count, Actions
- Filters: Category
- Editor: Name, description, category, featured image

Wire into `src/pages/admin/treatments.astro`.
</action>
  <verify>
grep "AdminTreatments" src/pages/admin/treatments.astro
</verify>
<done>Treatments page shows all treatments with edit capabilities</done>
</task>

<task type="auto">
  <name>Task 9: Build Audit Log bridge (BRIDGE-09)</name>
  <files>
    src/pages/api/admin/audit-log.ts
    src/components/react/AdminAuditLog.tsx
    src/pages/admin/audit-trail.astro
  </files>
  <action>
Check if audit-log API exists. If not, create `src/pages/api/admin/audit-log.ts`:
- GET: List audit log entries with actor, action, entity, timestamp
- Filters: By actor, action type, entity type, date range

Create `AdminAuditLog.tsx`:
- Table: Timestamp, Actor, Action, Entity type, Entity, Details
- Filters: Date range, Actor, Action type
- Expandable rows showing full details JSON
- Export to CSV

Wire into `src/pages/admin/audit-trail.astro`.
</action>
  <verify>
grep "AdminAuditLog" src/pages/admin/audit-trail.astro
</verify>
<done>Audit trail page shows all admin actions with detailed filtering</done>
</task>

<task type="auto">
  <name>Task 10: Build Newsletter bridge (BRIDGE-10, BRIDGE-11)</name>
  <files>
    src/pages/api/admin/newsletter.ts
    src/components/react/AdminNewsletter.tsx
    src/pages/admin/newsletter.astro
  </files>
  <action>
Fix `src/pages/api/admin/newsletter.ts` to handle all CRUD operations:
- GET: List subscribers with stats
- POST: Add subscriber, Unsubscribe, Resubscribe
- PUT: Update subscriber details
- DELETE: Delete subscriber

Fix the newsletter Astro page to use a proper React component instead of inline scripts.

Create `AdminNewsletter.tsx`:
- Tabs: Subscribers, Campaigns, Settings
- Subscribers: Table with email, name, status, subscribed date, actions
- Campaigns: List of email campaigns with stats
- Settings: Provider configuration, default sender
</action>
  <verify>
grep "AdminNewsletter" src/pages/admin/newsletter.astro
</verify>
<done>Newsletter page fully functional with subscriber and campaign management</done>
</task>

</tasks>

<verification>
- All 11 pages have functional React components
- All APIs handle CRUD operations
- Data flows correctly between UI and backend
</verification>

<success_criteria>
1. Subscriptions page shows all clinic subscriptions
2. Jobs applications page shows all applications with status
3. Notifications page shows notifications with read/unread
4. Billing page shows invoices and revenue
5. GDPR page shows requests with approve/reject
6. Integrations page shows connected services
7. Questions page shows quiz questions
8. Treatments page shows treatments
9. Audit log page shows all actions
10. Newsletter page fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/10-admin-panel-completeness/10-02-SUMMARY.md`
</output>
